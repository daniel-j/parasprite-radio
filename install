#!/bin/bash

# http://stackoverflow.com/a/246128
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR

function createConfig {
	echo "Creating config files..."
	cp -n config.default.toml config.toml
	cp -n liq/credentials.default.liq liq/credentials.liq
}

echo "Installing dependencies and building Parasprite Radio..."

createConfig

echo "Installing node dependencies..."
npm install

echo "Building app for development"
gulp

echo "Done! Now edit config.json, setup Redis and a MySQL server. Start server with ./radio -d (remove the -d when in production)"



